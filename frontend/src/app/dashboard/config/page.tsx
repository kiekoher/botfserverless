export default function ConfigPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">Agent Configuration</h1>
      <p>This page will contain the form to define the agent's name, product/service, and system prompt.</p>
    </div>
  );
}
