# Production Environment Variables
SUPABASE_URL="your_supabase_url_here"
SUPABASE_KEY="your_supabase_service_role_key_here"
SUPABASE_SERVICE_KEY="your_supabase_service_key_for_workers"
SUPABASE_JWT_SECRET="your_supabase_jwt_secret"
REDIS_HOST="redis"
REDIS_PASSWORD="change_me"
FRONTEND_ORIGINS="http://localhost:3000"

# Google AI Configuration (for backend)
GOOGLE_API_KEY="your_google_api_key_here"
GEMINI_EMBED_MODEL="models/embedding-001"
GEMINI_CHAT_MODEL="gemini-1.5-flash"
OPENAI_API_KEY="your_openai_key_here"
DEEPSEEK_API_KEY="your_deepseek_key_here"
OPENAI_EMBED_MODEL="text-embedding-3-large"

# WhatsApp Adapter Configuration
BACKEND_URL="http://main-api:8000/api/chat"

# Supabase for Frontend Auth
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
NEXT_PUBLIC_API_URL=http://main-api:8000/api/v1

# R2 / S3 Storage Configuration
R2_ENDPOINT_URL="https://example.r2.cloudflarestorage.com"
R2_BUCKET_NAME="your-bucket-name"
R2_ACCESS_KEY_ID="your_r2_access_key"
R2_SECRET_ACCESS_KEY="your_r2_secret_key"

# WhatsApp Gateway configuration
WHATSAPP_USER_ID="1234567890"

# Grafana admin password for the monitoring stack
GRAFANA_ADMIN_PASSWORD="supersecret"

# Traefik Configuration
DOMAIN_NAME="your_domain.com"
CERTBOT_EMAIL="your_email@example.com"

# --- Optional: Automated Backup Configuration ---
# The following variables configure the automated backup service (`cron/backup.sh`).
# To enable off-site backups to an S3-compatible remote (like Cloudflare R2),
# you must provide the rclone remote name and the bucket path.
# The rclone configuration file itself should be provided separately (e.g., via GitHub secrets).

# The name of the rclone remote to use for the upload (defaults to 'r2' in the script).
R2_REMOTE_NAME="r2"

# The bucket and path to upload backups to (e.g., "my-bucket/eva/backups").
# If this is left empty, off-site backups will be disabled.
R2_BUCKET_PATH=""

# Optional: A health check URL (e.g., from Healthchecks.io or Uptime Kuma)
# to ping after a successful backup run. This allows you to monitor that your backups
# are running on schedule. If left empty, this feature is disabled.
BACKUP_HEALTHCHECK_URL=""
